//your JS code here. If required.
let appName = window.navigator.appName;
let version = window.navigator.appVersion;
document.getElementById("browser-info").innerHTML = `You are using ${appName} version ${version}`
